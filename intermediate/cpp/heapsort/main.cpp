#include "heapsort.h"

#include <iostream>

int main() {
    const int N = 5;
    int array[N] = {4, 10, 3, 5, 1};

    print(array, N);

    std::cout << "Hello, World!" << std::endl;
    return 0;
}
